<%= render :partial => '/admin/shows/sub_header'%>
<h1> New Trade Show </h1>

<%= form_for(@trade_show, :url => admin_shows_trade_shows_path(@trade_show)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

  <%= link_to 'Back to List', admin_shows_trade_shows_path, :class => 'button' %>
