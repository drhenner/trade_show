<h1> <%= @trade_show.name %> </h1>

<%=raw @trade_show.description %>

<% content_for :sidebar do -%>
  <label>Trade Show Info</label>
  <ul>
    <li><%= link_to 'Virtual Trade Show' %></li>
  </ul>


  <label>Booths</label>
  <ul>
    <% @trade_show.booths.each do |booth| %>
        <li><%= link_to booth.name, booth_path(booth) %></li>
    <% end %>
  </ul>

<% end %>
